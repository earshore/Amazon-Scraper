# 2026-07-16

## 任务：把 Amazon Scraper Chrome 插件改造为免安装 Agent Web 应用

- 用 `codebuddy-chat-web` skill 的 `copy-template.sh` 在 `web/` 脚手架（React+Vite+Express+Tailwind）。
- 适配：移除聊天/CodeBuddy Agent SDK(`@tencent-ai/agent-sdk`)/better-sqlite3/`@tdesign-react/aigc|chat` 等脆弱依赖，
  改用精简依赖（cheerio 解析 + express API + 自定义 Tailwind/CSS UI），保证 `npm install` 可靠、应用免装即跑。
- 核心抓取逻辑 `content.js:scrapeAmazonLogic` 端口到 `web/server/scraper.ts`（cheerio），
  完整保留选择器、`closest` 区域过滤、跨欧洲日期/星级/来源国解析；输出结构与原插件一致。
- 新增 `POST /api/scrape {asin,domain}`，校验 ASIN、带浏览器头与 25s 超时拉取、`isRobotCheckPage` 检测 CAPTCHA、
  返回 423。`web/src/ScraperPage.tsx` 实现 输入ASIN→抓取→展示→导出JSON，含明暗主题切换。
- 验证：`tsc -b && vite build` 通过；合成 HTML fixture 解析正确（星级 4.5/3.0、国家提取 OK）；
  实拉 Amazon 已验证网络可达且错误优雅处理（404/robot-check）。
- 关键坑：cheerio 的 `cheerio.load()` 返回 `CheerioAPI`，文档级查询用 `$.root().find(sel)` 而非 `$.find`；
  `tsconfig.node.json` 需显式 `target/lib: ES2020` 否则 Set 展开/Object.entries 报错。
- 限制：Amazon 服务端请求常被反爬拦截（423），已在 README 说明，建议加前置代理。
